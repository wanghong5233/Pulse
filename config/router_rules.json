{
  "_comment_purpose": "Legacy slash-command router (see docs/Pulse-DomainMemory与Tool模式.md §3.4).",
  "_comment_status": "Being phased out. Natural-language inputs go directly to Brain; only slash-command shortcuts remain here. Modules with full IntentSpec coverage (job_*, intel) do not need entries.",
  "_comment_migration": "intel.* entries removed when the intel module migrated to a single deterministic-workflow IntelModule with IntentSpec tools (intel.digest.* / intel.search). Slash shortcuts that hit the same tools live below for muscle-memory users.",

  "fallback_intent": "general.default",
  "fallback_target": "hello",
  "intents": {
    "general.default": "hello",
    "email.process": "email_tracker",
    "email.fetch": "email_tracker",
    "intel.digest.list": "intel",
    "intel.digest.latest": "intel",
    "intel.digest.run": "intel",
    "intel.search": "intel"
  },
  "exact": [
    { "key": "ping", "intent": "general.default" },
    { "key": "/ping", "intent": "general.default" },
    { "key": "/intel digest", "intent": "intel.digest.list" },
    { "key": "/intel list", "intent": "intel.digest.list" }
  ],
  "prefix": [
    { "prefix": "/email process", "intent": "email.process" },
    { "prefix": "/email fetch", "intent": "email.fetch" },
    { "prefix": "/intel digest list", "intent": "intel.digest.list" },
    { "prefix": "/intel digest latest", "intent": "intel.digest.latest" },
    { "prefix": "/intel digest run", "intent": "intel.digest.run" },
    { "prefix": "/intel search", "intent": "intel.search" }
  ]
}
